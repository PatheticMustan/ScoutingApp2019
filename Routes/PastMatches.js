import React from 'react';
import {
    View,
    ScrollView
} from 'react-native';

export default class PastMatches extends React.Component {
    render() {
        return (
            <ScrollView>
                <View>
                </View>
            </ScrollView>
        );
    }
}